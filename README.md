# vue-mask-test

A Vue component to vue-mask-test

## Install

```JS
npm install vue-mask-test -S
```

## Use

```js
import Mask from 'vue-mask-test'

Vue.use(Mask)


// 组件调用
this.$mask.show()
this.$mask.hide()
```